# Copy this file to credentials.toml and fill in both values.
# credentials.toml is git-ignored — your tokens never leave your machine.
#
# jira_api_token:
#   https://id.atlassian.com/manage-profile/security/api-tokens
#   -> Create API token -> label it "TimeTracker" -> copy the value below.
#
# tempo_api_token:
#   In Jira: Apps -> Tempo -> Settings -> API integration
#   -> New token -> scope "Log work" / read+write -> copy the value below.

jira_api_token  = ""
tempo_api_token = ""
